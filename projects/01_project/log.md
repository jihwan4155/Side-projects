# 📅 Project Log: 01_project

## 📝 데일리 기록

### 2026-03-30 (Day 1)
- **오늘의 목표:** 프로젝트 레포 생성, 첫 프로젝트 아이디어 구상-> **'나만의 뉴스 웹사이트'**
점진적 실력 향상을 위해 간단한 목표부터 시작. (금융, 경제) 뉴스 클롤링 웹사이트 설계 및 구축.

- **진행 상황:** 
    1. 네이버 개발자 센터 애플리케이션 등록 및 API Key 발급
    2. `.env` 파일을 활용해 민감한 인증 정보를 코드와 분리
    3. `requests`라이브러리를 사용하여 네이버 뉴스 검색 API 호출 성공
    4. JSON 데이터에서 뉴스 제목과 링크 추출 완료

- **오늘의 도전 & 해결 (TIL):** 
전체를 바이브 코딩으로 하는 것이 아니라, 스스로 학습을 하며 개발하는 목표. AI는 검색 엔진 형식으로 활용.
    - 문제: 첫 API 호출 시 `400 Bad Request` 에러 발생
    - 해결: `response.text`를 출력해보니 필수 파라미터가 누락되었음을 확인. -> `params`딕셔너리에 검색어 담아 보내서 해결
- **다음 할 일:** 
    - 불러온 뉴스를 컴퓨터에 영구적으로 기록할 SQLite DB 설계. 
    - 뉴스 제목에 포함된 HTML 태그 클리닝 작업
---
### 2026-03-31 (Day 2)
- **오늘의 목표:**
  - 데이터를 보관하기 위한 SQLite 데이터베이스 연동
  - 뉴스 데이터를 관리할 Table 스키마 설계 및 생성
- **진행 상황:**
  - `sqlite3` 모듈로 DB 연결 성공
  - 뉴스 저장용 `news` 테이블 생성
  - `is_read` 컬럼에 `DEFAULT 0` 을 설정 해서 읽기 습관 트래킹 기반 마련
  - SQL의 `INSERT` 문법과 보안을 위한 파라미터 쿼리(`?`) 익힘
- **오늘의 도전 & 해결 (TIL):**
  - 문제: DB 파일이 프로젝터 폴더가 아닌 상위 폴더에 생성되는 문제 발생
  - 해결: `os.path` 를 활용해 `main.py` 위치 기준의 절대 경로로 DB를 연결하여 해결
- **다음 할 일:**
  - DAY1에 만든 네이버 API 호출 로직과 DAY2 에 만든 저장 로직 합치기
  - 사용자가 등록한 '관심 키워드'를 관리하는 `keywords` 테이블 추가 생성
  - 수집된 뉴스를 터미널이나 웹 화면에 구현 시작
---
### 2026-04-02 (Day 3)
- **오늘의 목표:**
  - API 수집 데이터와 SQLite 연동 및 자동 저장 로직(ETL) 구현
  - 다중 키워드 수집을 위한 `keywords` 테이블 연동 및 자동화
- **진행 상황:**
  - 기능의 모듈화: `fetch_and_save_news(keyword)` 함수를 만들어 재사용성 확보
  - 데이터 클리닝 심화: 뉴스 제목 내 불필요한 HTML 태그와 특수문자를 제거
  - 키워드 관리 자동화: 코드에 검색어를 직접 쓰지 않고, DB에서 키워드 목록을 불러와 반복문으로 전체 수집하도록 개선
  - 중복 방지: `INSERT OT IGNORE` 를 통해 이미 저장된 뉴스는 건너뛰고 새로운 뉴스만 저장하는 로직 궇녀
- **오늘의 도전 & 해결 (TIL):**
  - 문제: 뉴스 수집을 반복할 때마다 중복 데이터 처리가 까다로웠음
  - 해결: SQL의 `UNIQUE` 제약 조건과 `IGNORE` 키워드를 활용해 코드 복잡도를 낮추고 데이터 무결성 지킴
- **다음 할 일:**
  - DB에 쌓인 뉴스들을 조건에 맞춰(최신순, 안 읽은 순 등) 출력하는 조회 기능 구현
  - 특정 뉴스를 읽었을 때 `is_read` 상태를 `1`로 바꾸는 업데이트 기능 구현
---
### 2026-04-04 (Day 4)
- **오늘의 목표:**
  - 수집된 데이터를 관리하는 CRUD 기능 구현
  - 사용자 입력을 받아 동작하는 터미널 기반 메뉴 (CLI) 구축
  - `is_read` 상태를 활용한 유스 읽기 습관 트래킹 로직 완성
- **진행 상황:**
  - 메인 루프 구축: `while True` 문을 사용하여 사용자가 종료를 선택할 때까지 프로그램이 유지되도록 설계
  - 조회(READ) 기능: `WHERE is_read = 0` 조건을 사용해 아직 읽지 않은 뉴스만 필터링하여 보여주는 `get_unread_news()` 함수 구현
  - 수정(Update) 기능: 사용자가 입력한 특정 ID의 뉴스 상태를 '읽음'으로 변경하는 `mark_as_read()` 함수 구현
- **오늘의 도전 & 해결 (TIL):**
  - 문제: input()으로 받은 숫자로 메뉴를 선택할 때 자꾸 else 문(잘못된 입력)으로 빠지는 현상 발생.
  - 해결: input()은 데이터를 항상 문자열(str)로 받는다는 점을 확인. if choice == 1 대신 if choice == "1"로 비교하거나 int()로 형변환
- **다음 할 일:**
  - 뉴스를 읽을 때 단순히 상태만 바꾸는게 아니라, 실제 브라우저로 링크를 열어주는 기능추가
  - 수집된 뉴스 중 특정 단어가 포함된 뉴스만 골라내는 검색 기능
---
### 2026-04-07 (Day 5)
- **오늘의 목표:**
  - 수집된 뉴스를 브라우저에서 즉시 확인하는 자동 실행 기능 추가
  - 뉴스 제목 검색 기능 구현
  - 사용자 편의를 위한 CLI 메뉴 확장 및 로직 최적화
- **진행 상황:**
  - 브라우저 연동: webbrowser 라이브러리를 도입하여 뉴스 ID 입력 시 해당 기사의 URL을 기본 브라우저로 자동 실행하는 read_news() 함수 완성
  - 스마트 검색 엔진: SQL의 와일드카드(%)를 활용해 제목의 어느 위치에든 키워드가 포함되면 찾아내는 search_news() 로직 구현
  - 메뉴 체계 고도화: 기존 4개 메뉴에서 '검색'과 '브라우저 읽기'를 추가한 6단계 메뉴 시스템으로 개편
- **오늘의 도전 & 해결 (TIL):**
  - 문제: 함수를 호출할 때 정의된 이름과 호출하는 이름이 달라 발생하는 `NameError`
  - 해결: 코드의 규모가 커질수록 함수 네이밍 컨벤션(Naming Convention)과 일관성이 얼마나 중요한지 체감 
- **다음 할 일:**
  - 뉴스가 수집된 '시간'을 기준으로 정렬하여 가장 최신 기사부터 보여주는 기능 추가
  - 키워드별로 뉴스 개수를 통계 내어 보여주는 대시보드 리포트 기능 구상
---
### 2026-04-08 (Day 6)
- **오늘의 목표:**
  - 데이터 통계 및 시각화 기능 구현
  - 최신순 정렬
- **진행 상황:**
  - 실시간 대시보드: 프로그램 시작 시 상단에 `COUNT` 함수를 활용한 전체/읽음 통계 자동 출력
  - 데이터 정렬: 수동으로 최신 뉴스를 찾는 번거로움 제거
  - 명령어 추가: 특정 키워드의 비중을 계산하는 로직 도입
- **오늘의 도전 & 해결 (TIL):**
  - 문제: `GROUP BY`를 썼는데 결과가 모두 1로 나옴
  - 해결: 그룹화는 중복값이 있는 컬럼을 기준으로 해야 함. 현재 테이블 구조의 한계를 파악하여 로직을 우회함.
- **다음 할 일:**
  - 자동 수집 기능: 웹 페이지 구현 및 웹 페이지 접속시 뉴스 수집 함수 자동으로 실행 로직 구현
  - 메인 뉴스 배치: 메인 화면 중앙에 그날의 핵심 뉴스를 출력
  - UI 레이아웃 설정
---
### 2026-04-11 (Day 7)
- **오늘의 목표:**
  - 터미널 기반 로직을 Flask 웹 서버 환경으로
  - 프로그램의 실행 로직과 기능 정의 로직 분리 (모듈화)
  - 웹 브라우저를 통한 DB 뉴스 데이터 출력 
- **진행 상황:**
  - 서버 구축: Flask 프레임워크 활용해 로컬 서버 구동, templates 폴더 구조를 통해 HTML 문서 렌더링
  - 코드 격리: `if __name__ == "__main__":` 으로 `import`시 불필요한 코드 실행 현장 해결
  - DB 연동: SQLite 데이터를 HTML으로 화면에 리스트로 나타냄
- **오늘의 도전 & 해결 (TIL):**
  - 문제: `app.py`를 실행했을 때 웹 서버가 뜨지 않고 기존의 터미널 뉴스 수집 로직이 먼저 실행됨
  - 해결: 모든 실행 코드를 `if __name__ == "__main__" `안으로 옮겨 호출 시에는 함수만 빌리도록 수정함
- **다음 할 일:**
  - Bootstarp 디자인: CSS 프레임 워크 적용해 UI 개선
  - 레이아웃 설계: 사이드바와 메인 콘텐츠 영역을 구분하여 화면 구성
  - 네비게이션: 메뉴 버튼들을 배치하고 각 기능으로 연결되는 설계
---
### 2026-04-16 (Day 8)
- **오늘의 목표:**
  - Bootstrap을 활용한 현대적인 대시보드 UI 레이아웃 구축
  - 핵심 정보를 파악할 수 있는 '히어로 섹션' 구현
- **진행 상황:**
  - UI 프레임워크 도입: Bootstrap 5 CDN을 연동하여 웹 페이지 디자인 기초 마련.
  - 레이아웃 분리: row와 col 시스템을 이용해 사이드바(설정)와 메인(콘텐츠) 영역을 배분
  - 히어로 섹션: 가장 최신 뉴스를 강조하는 상단 배너 구현
  - 반응형 카드: 뉴스 목록을 카드 형태로 배치
- **오늘의 도전 & 해결 (TIL):**
  - 문제: 키워드와 제목을 하나하나 직접 HTML에 입력하다보니 반복 작업이 되어 시간이 많이 소요되고 효율성이 떨어짐
  - 해결: `{% for %}` 과 `{% if %}`문법을 학습하여 카드들을 자동 생성함
- **다음 할 일:**
  - 사이드바 동적 연동: 사이드바 키워드를 실제 DB의 `keywords`테이블 데이터로 교체
  - 키워드 필터링: 사이드바의 특정 키워드를 클릭했을 때 해당 뉴스만 모아보는 쿼리 연동
  - UI 디테일: 전체적인 UI를 좀 더 보기 좋게 하기
---
### 2026-04-20 (Day 9)
- **오늘의 목표:**
  - 사이드바 키워드 목록의 동적 연동
  - 키워드 필터링 엔진 구현
- **진행 상황:**
  - `request.args.get()`을 사용하여 사용자가 클릭한 키워드 값을 서버가 인식하도록 로직 설계
  - SQL의 `LIKE`연산자와 와일드카드를 결합해 사용자가 선택한 키워드가 포함된 뉴스만 골라내는 필터링 시스템 구축
  - `{% for %} 루프를 사용해 keywords 테이블의 데이터를 사이드바에 자동으로 보이게 함
- **오늘의 도전 & 해결 (TIL):**
  문제: 특정 키워드 선택시 해당 키워드 강조가 가능하지만, 전체보기에서는 어떤 피드백을 줘야할지
  해결: `<a href="/"` 통해 모든 파라미터를 제거하고 초기 상태로 되돌리는 기능을 구현 
- **다음 할 일:**
  - 터미널이 아닌 웹 화면에서 키워드를 직접 추가/삭제하는 기능
  - 키워드 추가 시 화면 새로고침 없이 반영하기
  - 필터링된 결과 중 최신 뉴스가 히어로 섹션으로 노출
---
### 2026-04-21 (Day 10)
- **오늘의 목표:**
  - 웹 인터페이스를 통한 키워드 추가/삭제 기능 
  - HTTP POST 메서드를 활용한 서버 데이터 변경 및 리다이렉션 구현
- **진행 상황:**
  - 키워드 관리 시스템: 웹 브라우저에서 키워드를 직접 입력하면 DB의 `keywords`테이블에 저장되고, 사이드바에 즉시 업데이트되는 흐름 완성
  - 동적 라우팅: 삭제 기능을 구현할때 URL자체를 데이터 전달 통로로 활용
- **오늘의 도전 & 해결 (TIL):**
  - 문제: `execute()` 함수에 데이터를 하나만 넘길 때, 파이썬이 데이터를 한 글자씩 쪼개버리는 현상
  - 해결: Trailing Comma `(data,)` 문법을 사용하여 튜플로 선언. 파이썬 문법의 작은 차이가 시스템에 미친 영향을 체감함
- **다음 할 일:**
  - 수집 버튼 연동: 웹 화면에서 '수집 시작' 버튼으로 뉴스 크롤링 함수를 호출
  - 사용자 피드백: 수집이 진행되는 동안 "수집 중.."임을 알리는 로딩 상태 표시 고민
  - 키워드 통계: 각 키워드별로 현재 DB에 저장된 뉴스 개수를 사이드바에 숫자로 노출
---
### 2026-04-11 (Day 11)
- **오늘의 목표:**
  - 뉴스 수집 버튼 구현
  - 키워드별 뉴스 통계 구현
  - 사이드바 레이아웃 디테일 최적화
- **진행 상황:**
  - 한 번으로 `main.py`의 크롤링 로직이 구동되도록 연동함. 터미널 없이 수집 프로세스 제어 가능
  - 뉴스 테이블과 키워드를 결합해 각 키워드에 속한 뉴스 개수를 실시간으로 계산
  - 키워드 이름, 삭제 버튼이 수평으로 정렬되도록 수정함
- **오늘의 도전 & 해결 (TIL):**
  - 문제: 사이드바에서 코드가 길어지자 정렬이 되지 않아 레이아웃이 깨짐
  - 해결: Flexbox의 `flex-grow-1` 속성을 활용해 클릭 영역은 최대한 넓히고, 보조 버튼(삭제)은 오른쪽 끝으로 밀어내는 배치
- **다음 할 일:**
  - 네이버 API가 제공하는 날짜 형식을 읽기 편한 형태로 가공
  - 이미 읽은 뉴스는 읽은 표시를 함으로써 시각적으로 구분
  - 특정 키워드에 뉴스가 없을 때 수집하라는 버튼과 안내메시지 구현
---
### 2026-04-23 (Day 12)
- **오늘의 목표:**
  - 뉴스 날짜 표시를 `YYYY-MM-DD HH:MM`으로 변환
  - 뉴스 클릭스 '읽음' 처리 및 시각적 피드백 제공
  - 데이터가 없는 상황(Empty State)에 대한 UX 예외 처리
- **진행 상황:**
  - 데이터 정제: 파이썬 `datetime` 라이브러리를 활용해 RFC 822 날짜를 `2026-04-23 20:44` 형식으로 변환 저장
  - 상태 업데이트: `/read_news/<id>` 라우트를 구현하여 클릭 시 DB의 is_read 값을 1로 변경하고 원문으로 리다이렉트
  - UI 반영: 읽은 뉴스에 '읽음' 배지를 적용해 시각적 피드백 제공
  - 예외 처리: 뉴스가 없을 때 안내 메시지와 수집 유도 버튼 노출
- **오늘의 도전 & 해결 (TIL):**
  - 문제 1: Cannot operate on a closed database 에러와 함께 수집이 중단됨. 원인은 전역 커서 사용 및 반복문 내 이미 `conn.close()` 실행이었음
  - `get_db_connection()` 함수를 통해 필요할 때만 연결을 생성하고, 작업이 완전히 끝난 뒤 닫도록 로직을 전면 수정함
  - 문제 2: 뉴스를 클릭했는데 비정상적인 URL이 생성됨. 원인은 URL 파라미터로 ID가 아닌 전체 링크(String)를 보냈기 때문
  - 해결 2: HTML 템플릿에서 news['id']를 넘기도록 수정하고, 서버에서 이 ID로 DB를 조회해 리다이렉트함
- **다음 할 일:**
  - 에러 핸들링: API 키 만료나 네트워크 오류 발생 시 사용자에게 알림 띄우기
  - 검색 기능 확장: 제목뿐만 아니라 내용에서도 키워드 검색이 가능하게 쿼리 보완
  - 전반적인 테마 색상 조정

---
### 2026-04-27 (Day 13)
- **오늘의 목표:**
  - 사용자 피드백 강화를 위해 알림 시스템 구축
  - 검색 범위를 내용(줄거리)까지 확장하여 검색 엔진 고도화
- **진행 상황:**
  - Flask `flash`와 부트스트랩 `alert` 컴포넌트를 연동하여 뉴스 수집 성공/실패 시 상단 알림창이 뜨도록 구현
  - 뉴스 요약본 저장을 하기 위해 DB 스키마에 `description`컬럼을 추가
  - 뉴스 카드에 요약 글 영역을 추가
- **오늘의 도전 & 해결 (TIL):**
  - 문제: `description` 컬럼 추가를 위해 기존 DB 파일 삭제했더니 서버 실행시 `no such table: keywords` 에러 발생
  - 해결: `main.py`에서 기본 DB 생성 함수 호출 후 전체 스키마를 미리 다시 구축함. Migration시 초기화 과정이 필요했음.
- **다음 할 일:**
  - 배포 준비
---
### 2026-04-28 (Day 14)
- **오늘의 목표:**
  - UI/UX 최종 폴리싱 
- **진행 상황:**
  - 히어로 섹션 리뉴얼: 배경색과 텍스트 대비를 높여 가독성을 확보하고 중앙 정렬
  - 데이터 전처리 보완: 특수문자가 깨지던 현상 해결
- **오늘의 도전 & 해결 (TIL):**
  - 문제: 뉴스 내용에 HTML 코드가 그대로 노출되는 현상 발생
  - 해결: `html` 라이브러리 사용하고 '언이스케이프' 과정 추가
- **다음 할 일:**
  - 프로젝트 종료
  - 확장 계획: AI API 연동하여 뉴스 요약 기능 + 자동 수집 기능 구체화




