# 📅 Project Log: 01_project

## 📝 데일리 기록

### 2026-03-30 (Day 1)
- **오늘의 목표:** 프로젝트 레포 생성, 첫 프로젝트 아이디어 구상-> **'나만의 뉴스 웹사이트'**
점진적 실력 향상을 위해 간단한 목표부터 시작. (금융, 경제) 뉴스 클롤링 웹사이트 설계 및 구축.

- **진행 상황:** 
    1. 네이버 개발자 센터 애플리케이션 등록 및 API Key 발급
    2. `.env` 파일을 활용해 민감한 인증 정보를 코드와 분리
    3. `requests`라이브러리를 사용하여 네이버 뉴스 검색 API 호출 성공
    4. JSON 데이터에서 뉴스 제목과 링크 추출 완료

- **오늘의 도전 & 해결 (TIL):** 
전체를 바이브 코딩으로 하는 것이 아니라, 스스로 학습을 하며 개발하는 목표. AI는 검색 엔진 형식으로 활용.
    - 문제: 첫 API 호출 시 `400 Bad Request` 에러 발생
    - 해결: `response.text`를 출력해보니 필수 파라미터가 누락되었음을 확인. -> `params`딕셔너리에 검색어 담아 보내서 해결
- **내일의 할 일:** 
    - 불러온 뉴스를 컴퓨터에 영구적으로 기록할 SQLite DB 설계. 
    - 뉴스 제목에 포함된 HTML 태그 클리닝 작업
---
### 2026-03-31 (Day 2)
- **오늘의 목표:**
  - 데이터를 보관하기 위한 SQLite 데이터베이스 연동
  - 뉴스 데이터를 관리할 Table 스키마 설계 및 생성
- **진행 상황:**
  - `sqlite3` 모듈로 DB 연결 성공
  - 뉴스 저장용 `news` 테이블 생성
  - `is_read` 컬럼에 `DEFAULT 0` 을 설정 해서 읽기 습관 트래킹 기반 마련
  - SQL의 `INSERT` 문법과 보안을 위한 파라미터 쿼리(`?`) 익힘
- **오늘의 도전 & 해결 (TIL):**
  - 문제: DB 파일이 프로젝터 폴더가 아닌 상위 폴더에 생성되는 문제 발생
  - 해결: `os.path` 를 활용해 `main.py` 위치 기준의 절대 경로로 DB를 연결하여 해결
- **내일의 할 일:**
  - DAY1에 만든 네이버 API 호출 로직과 DAY2 에 만든 저장 로직 합치기
  - 사용자가 등록한 '관심 키워드'를 관리하는 `keywords` 테이블 추가 생성
  - 수집된 뉴스를 터미널이나 웹 화면에 구현 시작
---
